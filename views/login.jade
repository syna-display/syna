extend layout
block imports
    script(src="/js/jquery.min.js")
    script(src="/js/login.js")
block content
    .container.content
        .row
            img.img-responsive.center-block(src="/img/logo.png")
        .row
            .col-lg-12
                h1.page-header #{__("Connection")}
        .row
            .col-lg-12
                each error in errors
                    .error
                        div #{error}
                form(role="form", action="/login", method="post")
                    .form-group
                        label(for="code")
                            | #{__("Code")}
                        #code-input
                            input.form-control(id="code", name="code", type="text")
                            a(href="/api/v1/displayOverlay") Afficher le code à l'écran
                    .form-group
                        label(for="username")
                            | #{__("Username")}
                        input.form-control(id="username", name="username", type="text")
                    button.btn.btn-default(type="submit")
                        | #{__("Sign in")}
